server:
  port: 8001

spring:
  application:
    name: MOVIE-SERVICE

    # Database
  datasource:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/userdb
      password: 123
      username: postgres

  jpa:
      generate-ddl: true
      open-in-view: false
      database: postgresql
      show-sql: true
      hibernate:
        ddl-auto: update



